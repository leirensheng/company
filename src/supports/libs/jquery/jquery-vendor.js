/**
 * Created by clude on 10/8/16.
 */
//require('jquery');
//req = require.context('./plugins', true, /\.js$/);
//req.keys().forEach(function (plugin) {
//    req(plugin);
//});
//
//module.exports = jQuery;


import $ from 'jquery';
window.$ = $;
window.jQuery = $;

export default $